import React from "react";

// this type of file is known as layout files
// this loading page will be shown when user visits contact page and it is not loaded initially
const Loading = () => {
  return <div>Loading</div>;
};

export default Loading;
